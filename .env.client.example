# Client-Side Configuration for Ingestion GUI
# =============================================
# 
# Copy this file to .env and edit the SERVER_TAILSCALE_IP
# Get the IP from your team lead: run `tailscale ip -4` on the server
#
# After editing, restart ingestion GUI to connect to server

# Server API endpoint - CHANGE THIS
API_BASE=http://SERVER_TAILSCALE_IP:8000/api

# Local temp directory for downloads (relative to project root)
DATA_ROOT=./data
